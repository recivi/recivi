---
title: Home
description: >-
  Hello and welcome to my portfolio site!
ogTitle: ^•ﻌ•^𐒡
---

import reciviData from "virtual:recivi/data";
import { Comms } from "virtual:pf/components";
import { getArrayDisplay } from "@recivi/pf/utils/intl";
import { formatAddress } from "@recivi/schema/display";

import ManekiNeko from "@/components/ManekiNeko.astro";

export const { name, labels, residence, origin } = reciviData.bio;

# <ManekiNeko/>

Hello! I am **{ name }**, a { labels && getArrayDisplay(labels) }{ residence &&
` based in ${formatAddress(residence)}` }{ origin && ` (originally from ${formatAddress(origin)})`
}.

## Contact

You can get in touch with me through any of these channels:

<Comms />
