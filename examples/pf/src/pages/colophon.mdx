---
title: Colophon
description: >-
  Learn about the principles, design choices and technology behind this site.
navIndex: 4
---

import { Anchor, Brand } from "virtual:pf/components";
import nav from "virtual:pf/nav";
import projectContext from "virtual:pf/project-context";
import reciviData from "virtual:recivi/data";

import { getBlogPostUrl, getResumeEntityUrl } from "@recivi/pf/utils/dynamic_pages";
import { getUrlFromPattern, getUrlFromSlug } from "@recivi/pf/utils/project_context";

export const resumeIndexUrl = getUrlFromPattern(projectContext, nav.resume.index.pattern);
export const blogIndexUrl = getUrlFromPattern(projectContext, nav.blog.index.pattern);

# Colophon

A colophon is a record of how a publication was made. This one documents the principles, design
choices and technology behind this site.

## Principles

### Data first

At the heart of this site is a structured, machine-readable, JSON file that contains my résumé data.
This file complies with the <Brand icon={{ pack: "pf", name: "recivi" }}
anchor={{ url: "https://recivi.dhruvkb.dev" }} name="Récivi" /> specification.

The Récivi PF renderer parses that document, validates it and generates a web site out of it,
turning the same data into an interactive portfolio and a print-ready PDF of my résumé. This turns
the idea of how résumés have traditionally worked on its head. Instead of computers trying to
extract facts from a freely formatted document, they can just read the JSON file.

### IndieWeb

This site follows several [IndieWeb](https://indieweb.org/) principles: owning the domain, retaining
control of the underlying data, favouring open-source tools and keeping authored content in durable
plain-text formats. Contact and profile links use `rel="me"`, which lets compatible services such as
Mastodon verify that those profiles and this domain represent the same person through
[RelMeAuth](https://microformats.org/wiki/RelMeAuth).

[Webmention](https://indieweb.org/Webmention) is not implemented yet, but would be a natural next
step toward making the site a more active participant in the independent web.

### Progressive enhancement

The site's content, links and navigation begin as semantic HTML. A minimal amount of CSS supplies
the responsive layout, colour and print presentation. We sprinkle a little bit of client-side
interactivity using <Brand icon={{ name: "alpinedotjs" }} anchor={{ url: "https://alpinejs.dev/" }}
name="Alpine.js" />. This is completely optional and even with JavaScript disabled, the experience
of using the site remains mostly intact.

## Design

### Color palette

The colour palette comes from [Catppuccin](https://catppuccin.com/) using the Latte theme for light
mode and the Mocha theme for dark mode. The active flavour follows the visitor's system preference.
Syntax highlighting colours use the same theme, giving the site a cohesive visual language.

### Layout

The web layout is deliberately content-led. It begins as a single column on smaller screens,
introduces a fixed-width reading pane when space allows and can add a complementary second pane on
wider displays. The print layout is designed separately around A4 pages instead of asking the web
layout to masquerade as paper.

### Accessibility

The site tries to be as accessible as possible, with care taken to ensure semantic HTML, logical
page structure and sufficient contrast. Its reading-focused layout is designed to make reading prose
and code easier.

### Typography

#### Web

The site uses your default `serif`, `sans-serif` and `monospace` fonts for the headings, body and
code respectively. These fonts can be configured in the browser settings. There are a multitude of
reasons behind this design decision:

- Using system fonts reduces bandwidth use and improves the site's load time.
- It doesn't patronise you and respects the font choices you set in the browser.
- It accommodates readers who have chosen typefaces that meet their particular accessibility needs.

System font families vary by platform, and that variation is a feature: the page should feel at home
on the device displaying it.

#### Print

Print needs repeatable line lengths and page breaks, so the printable layouts use specific fonts.

- [Gelasio](https://github.com/sorkintype/Gelasio) by Eben Sorkin, for the headings and body
- [JetBrains Mono](https://www.jetbrains.com/lp/mono/) by JetBrains, for code

Both fonts are sourced from Google Fonts and are licensed under the SIL Open Font License 1.1.

## Technology

The site is a collection of largely static HTML pages which are generated automatically at build
time, from content written using <Brand icon={{ name: "mdx", title: "MDX" }}
anchor={{ url: "https://mdxjs.com/" }} name="MDX" />. MDX is the same Markdown we know and love, now
with the added ability to embed components.

### Récivi

This site is a combination of <Brand icon={{ name: "astro" }}
anchor={{ url: "https://astro.build" }} name="Astro" /> and the <Brand
icon={{ pack: "pf", name: "recivi", title: "Récivi" }}
anchor={{ url: "https://recivi.dhruvkb.dev" }} name="Récivi PF" /> renderer. Astro handles the
routing, content and build systems, while the PF renderer does the rest of the heavy lifting:

- It creates an <Anchor url={resumeIndexUrl}>interactive résumé</Anchor> with focused pages for
  education, work and projects.
- It turns routes with a `.pdf` into actual PDFs during the build.
- It creates a <Anchor url={blogIndexUrl}>blog/digital garden</Anchor> with features like categories
  and post series.
- It creates an RSS feed for the blog.
- It turns routes with a `.png` into actual images during the build.
- It adds metadata like canonical URLs and Open Graph images to all pages.
- It provides components and layouts to build your own pages.
- It exposes configuration hooks to deeply customise the site.

The result is opinionated enough to be useful immediately, but deliberately open at the edges. A
site can replace individual components, extend its styles or add ordinary Astro pages without
forking the integration.
